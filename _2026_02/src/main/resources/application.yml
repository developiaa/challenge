spring:
  datasource:
    url: ''
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        show_sql: true

# ??? ?? ??
sharding:
  datasources:
    ds-0:
      url: jdbc:mysql://localhost:3306/shop_db?serverTimezone=UTC&characterEncoding=UTF-8
      username: root
      password: password
      driver-class-name: com.mysql.cj.jdbc.Driver
    ds-1:
      url: jdbc:mysql://localhost:3307/shop_db?serverTimezone=UTC&characterEncoding=UTF-8
      username: root
      password: password
      driver-class-name: com.mysql.cj.jdbc.Driver
    ds-2:
      url: jdbc:mysql://localhost:3308/shop_db?serverTimezone=UTC&characterEncoding=UTF-8
      username: root
      password: password
      driver-class-name: com.mysql.cj.jdbc.Driver
  hikari:
    maximum-pool-size: 10
    minimum-idle: 5
    connection-timeout: 30000
